import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF1976D2);
  static const primaryLight = Color(0xFF42A5F5);
  static const primaryDark = Color(0xFF0D47A1);
  // === Backgrounds ===
  static const lightBackground = Color(0xFFF6F8FC);
  static const darkBackground = Color(0xFF0B1220);
  static const background = Color(0xFFF5F7FA);

  // === Surface ===
  static const lightCard = Colors.white;
  static const darkCard = Color(0xFF111A2E);
  static const card = Colors.white;

  // === Text ===
  static const lightTextPrimary = Color(0xFF1C1F2E);
  static const lightTextSecondary = Color(0xFF6B7280);

  static const darkTextPrimary = Color(0xFFE5E7EB);
  static const darkTextSecondary = Color(0xFF9CA3AF);

  // === Status ===
  static const success = Color(0xFF22C55E);
  static const warning = Color(0xFFF59E0B);
  static const error = Color(0xFFEF4444);

  // === Border ===
  static const borderLight = Color(0xFFE5E7EB);
  static const borderDark = Color(0xFF1F2A44);
}
