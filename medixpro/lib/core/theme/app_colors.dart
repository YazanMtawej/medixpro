import 'package:flutter/material.dart';

class AppColors {
  // ===================== PRIMARY (Medical Blue System) =====================
  static const primary = Color(0xFF1E88E5); // main blue
  static const primaryLight = Color(0xFF64B5F6);
  static const primaryDark = Color(0xFF0D47A1);
  static const primarySoft = Color(0xFFE3F2FD);

  // ===================== LIGHT MODE =====================
  static const lightBackground = Color(0xFFF6F9FC);
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightCard = Color(0xFFFFFFFF);

  static const lightGradientStart = Color(0xFFFFFFFF);
  static const lightGradientEnd = Color(0xFFEAF4FF);

  // ===================== DARK MODE (Indigo/Navy System) =====================
  static const darkBackground = Color(0xFF0A0F1E);
  static const darkSurface = Color(0xFF111A2E);
  static const darkCard = Color(0xFF16213A);

  static const darkGradientStart = Color(0xFF0A0F1E);
  static const darkGradientEnd = Color(0xFF111A2E);

  // ===================== TEXT =====================
  static const lightTextPrimary = Color(0xFF0F172A);
  static const lightTextSecondary = Color(0xFF64748B);

  static const darkTextPrimary = Color(0xFFE5E7EB);
  static const darkTextSecondary = Color(0xFF94A3B8);

  // ===================== STATUS =====================
  static const success = Color(0xFF22C55E);
  static const warning = Color(0xFFF59E0B);
  static const error = Color(0xFFEF4444);

  // ===================== BORDERS =====================
  static const borderLight = Color(0xFFE2E8F0);
  static const borderDark = Color(0xFF24324A);
}